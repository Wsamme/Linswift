# Linswift 开发日志

用于持续记录每次开发的目标、改动、经验、问题和项目资源。后续默认按日期追加，不另起零散文档。

## 记录规则

1. 按日期归档，格式统一为 `## YYYY-MM-DD`。
2. 每次开发至少记录：
   - 开发目标
   - 主要改动
   - 验证结果
   - 遗留问题
   - 经验总结
3. 涉及部署时，记录部署类型、地址、阻塞问题和处理方式。
4. 涉及数据库或存储结构调整时，记录对应迁移文件和影响范围。
5. 涉及外部资源时，统一维护在“项目资源”章节，避免散落。

## 项目资源

### 核心文档

- `README.md`
- `PRD.md`
- `DEVELOPMENT_PLAN.md`
- `PROJECT_STATUS_REPORT.md`
- `DATABASE_ALIGNMENT_CHECKLIST.md`
- `VOCAB_OPTIMIZATION_CHECKLIST.md`
- `GRAMMAR_OPEN_SOURCE_SUMMARY.md`
- `TEST_ACCOUNT.md`

### 关键目录

- `src/`：前端主代码
- `public/`：静态资源
- `public/classic-books/`：10 本公共经典书正文
- `supabase/migrations/`：正式迁移脚本
- `scripts/`：构建和资源预处理脚本
- `electron/`：桌面端入口

### 关键配置

- `package.json`
- `vite.config.ts`
- `vercel.json`
- `.vercelignore`
- `src/lib/supabase.ts`

### 部署资源

- 生产地址：[https://linswift-app.vercel.app](https://linswift-app.vercel.app)
- Vercel 项目：`linswift-app`
- Supabase：使用 `.env` 中配置的项目

---

## 2026-03-20

### 开发目标

- 补齐设置与学习流程中的一批产品调整。
- 清理 OCR 页面相关残留。
- 引入“公共经典书库”，让 10 本英文经典书对所有用户可见，同时避免重复占用 Supabase 空间。
- 完成最新版本部署。

### 主要改动

#### 1. 设置与学习功能调整

- 关于页相关内容补充。
- 拼写复习规则调整：
  - 允许拼写错误。
  - 可查看正确拼写。
  - 只要某个词今天拼错过一次，后续必须重新拼写。
  - 只有最终无错误通过，才算完成当日复习。
- 删除 OCR 功能页面入口。
- 语言选项收敛为：
  - 中文
  - 英语
  - 日语

#### 2. OCR 残留清理

- 清理 [`src/pages/PDFReaderPage.tsx`](./src/pages/PDFReaderPage.tsx) 中已删除 OCR 页面遗留的未使用状态、方法和类型引用。
- 解决 `tsc` 因未使用变量导致的构建失败问题。

#### 3. 公共经典书库

- 将 `public/10_Classic_English_Books.zip` 解压到 [`public/classic-books/`](./public/classic-books/)。
- 新增 [`src/data/classicBooks.ts`](./src/data/classicBooks.ts)，维护 10 本书的公共目录。
- 新增 [`src/lib/books.ts`](./src/lib/books.ts)，统一处理：
  - 公共书正文加载
  - Project Gutenberg 头尾清理
  - 共享书元数据补全
  - 书籍正文分析摘录
- 调整 [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)：
  - 新增“公共经典书库”区块
  - 支持“加入书架”
  - 若已加入则直接打开/继续阅读
- 调整阅读链路：
  - [`src/pages/ReadingPrepPage.tsx`](./src/pages/ReadingPrepPage.tsx)
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - [`src/pages/FlashcardPage.tsx`](./src/pages/FlashcardPage.tsx)
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
- 为 `user_books` 增加共享引用字段：
  - [`supabase/migrations/20260320113000_user_books_shared_library.sql`](./supabase/migrations/20260320113000_user_books_shared_library.sql)

### 存储方案总结

- 采用“静态正文单份存储 + Supabase 轻量引用”的方式。
- 10 本经典书正文只保留在 `public/classic-books/` 一份。
- `user_books` 只保存：
  - 用户与书的关系
  - 阅读进度
  - 个人状态
  - `shared_book_slug`
- 避免为每个用户复制 `content_text` 大字段，显著节省 Supabase 空间。

### 验证结果

- 本地构建通过：`npm run build`
- 预览部署完成。
- 生产部署完成，正式域名已更新：
  - [https://linswift-app.vercel.app](https://linswift-app.vercel.app)

### 部署记录

- 预览部署地址：
  - 已迁移到 `icecream` 名下 Vercel 项目，旧的 `wsammes-projects` 预览地址不再使用
- 正式部署地址：
  - [https://linswift-app.vercel.app](https://linswift-app.vercel.app)

### 遗留问题

- 用户反馈测试环境仍可能出现空白页，推测与此前同类问题一致，后续排查时应优先复用之前已知可复现的测试地址。
- 前端包体积仍偏大，Vite 构建存在 chunk size warning，后续可考虑拆包优化。

### 经验总结

- 删除功能页面后，必须同步清理引用链，尤其是 TypeScript 页面级状态、事件处理器和导入类型，否则很容易在部署前才暴露构建错误。
- 公共内容不要直接复制进 `user_books.content_text`，应优先建“共享内容 + 用户状态”结构，成本更低，也更利于后续扩展公共书库。
- 对于 Project Gutenberg 这类公开文本，接入前要先统一做头尾版权声明清理，否则会影响阅读体验和 AI 分析质量。
- 部署前先本地 `npm run build`，能显著减少 Vercel 上的无效排队和调试时间。

### 后续建议

- 增加“公共经典书库”与“我的书架”的筛选/分组逻辑，减少入口混杂。
- 为共享经典书补充封面图、简介、难度标签和推荐理由。
- 统一整理测试地址与复现步骤，单独沉淀成测试说明，避免每次靠口头回忆。

### 当日补充：公共经典书库 UI 调整

- 用户反馈公共经典书库卡片过大、留白过多，当前大白板式布局不可用。
- 已新增程序生成的插画风格封面组件：
  - [`src/components/books/ClassicBookCover.tsx`](./src/components/books/ClassicBookCover.tsx)
- 已扩展经典书目录元数据：
  - [`src/data/classicBooks.ts`](./src/data/classicBooks.ts)
  - 为每本书补充主题色、图案、标语
- 已改造 [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)：
  - 公共经典书库支持“封面视图 / 列表视图”切换
  - 视图偏好写入本地存储，刷新后保留
  - 封面墙改为更紧凑的 `5:7` 书封布局
  - 列表模式增加缩略封面、标语和紧凑操作按钮
- 本次改动后本地构建验证通过：`npm run build`

### 当日补充：打开书慢与陌生词分析进度

- 用户反馈“打开书很久，像是打不开”，排查后确认主要问题不是正文加载本身，而是页面把“加载正文”和“AI 分析陌生词”串成了一个阻塞流程。
- 主要修复：
  - [`src/pages/ReadingPrepPage.tsx`](./src/pages/ReadingPrepPage.tsx)
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - [`src/lib/books.ts`](./src/lib/books.ts)
- 调整内容：
  - 书籍正文加载完成后立即显示页面，不再等待 AI 分析结束
  - 陌生词分析改为后台进行
  - 新增分析进度条、状态文案和百分比
  - 阅读准备页即使还在分析，也允许用户直接进入阅读页
  - 新增会话级词汇缓存，避免同一本书在准备页和阅读页重复等待
- 经验：
  - 任何 AI 后处理都不应该阻塞“打开书”这种主路径动作
  - 用户可感知的进度反馈比单纯 spinner 更重要，尤其是在 2 秒以上等待中

### 当日补充：10 本经典书清洗、分章节与章节阅读器

- 用户需求：
  - 清洗 10 本公共经典书正文，去掉插图引用、目录噪声和非正文内容
  - 将正文按章节拆分
  - 设计新的章节阅读器，支持目录跳转、书签、同位置续读、悬浮单词卡
  - 陌生词识别支持按 `3/5/10` 页或按章节触发
  - 已识别记录需要持久化，并与书签联动

#### 主要改动

- 新增经典书预处理脚本：
  - [`scripts/prepare-classic-books.mjs`](./scripts/prepare-classic-books.mjs)
- 构建链路接入预处理：
  - [`package.json`](./package.json)
  - `dev / build / build:desktop` 均会先执行 `prepare:classic-books`
- 产出结构：
  - [`public/classic-books/processed/manifest.json`](./public/classic-books/processed/manifest.json)
  - [`public/classic-books/processed/`](./public/classic-books/processed/)
- 预处理规则补充：
  - 清理 Project Gutenberg 头尾说明
  - 跳过插图块、frontispiece、目录污染项
  - 针对不同书型分别处理：
    - 标准 `CHAPTER` 结构
    - `Sherlock Holmes` 独立故事结构
    - `Frankenstein` 书信 + 章节结构
    - `A Tale of Two Cities` 的 `Book + Chapter` 结构
  - 修复了 `Tom Sawyer`、`Dracula`、`Pride and Prejudice` 的正文入口误判问题
- 新增章节阅读基础能力：
  - [`src/lib/classicReader.ts`](./src/lib/classicReader.ts)
  - 提供：
    - 章节 JSON 加载
    - 章节分页
    - 识词范围计算
    - 本地状态持久化
    - 书签/识词记录结构
- 新增章节阅读器组件：
  - [`src/components/books/ClassicChapterReader.tsx`](./src/components/books/ClassicChapterReader.tsx)
  - 功能包括：
    - 章节目录侧栏
    - 书签侧栏
    - 识词设置侧栏
    - 阅读进度条
    - 页级切换
    - 悬浮单词卡
    - 书签与识词记录联动
- 阅读页接入新模式：
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - 共享经典书不再走旧的整本正文模式，而是直接加载章节 JSON
- 书架入口调整：
  - [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)
  - 公共经典书从书架打开后，直接进入章节阅读器，而不是旧的阅读准备页
- 共享书内容加载策略补充：
  - [`src/lib/books.ts`](./src/lib/books.ts)
  - 新增按需控制是否加载共享书全文，避免经典书阅读时再额外请求整本原始文本

#### 验证结果

- 章节预处理成功：
  - 10 本书均已生成 `processed/*.json`
  - 当前抽样结果为：
    - `alice-in-wonderland`: 12 章
    - `pride-and-prejudice`: 61 章
    - `sherlock-holmes`: 12 篇
    - `tom-sawyer`: 35 章
    - `frankenstein`: 28 章
    - `dracula`: 27 章
    - `jane-eyre`: 40 章
    - `moby-dick`: 139 章
    - `a-tale-of-two-cities`: 45 章
    - `dorian-gray`: 20 章
- 数据抽查通过：
  - 无空章节
  - `Tom Sawyer` 与 `Dracula` 已恢复为真实章节数
  - `Pride and Prejudice` 首章标题污染已修复
- 本地构建通过：
  - `npm run build`

#### 经验总结

- 章节清洗不能只靠一个通用正则，不同经典书的排版历史差异很大，必须按书型分类处理。
- 图书阅读器要避免“先加载整本，再分析整本”的思路；章节化后，阅读与识词都应该按局部范围工作。
- 对共享静态书正文，要区分“原始全文”和“处理后章节数据”两套资产：
  - 原始全文适合离线清洗与回退
  - 处理后 JSON 适合在线阅读
- 目录、书签、识词频率、识词结果如果不持久化，阅读器就会退化成一次性页面，无法形成连续学习体验。

#### 当前遗留

- 章节阅读器当前以本地持久化为主，后续可考虑将书签和识词记录进一步同步到 Supabase，支持多端续读。
- `Dracula` 的章节标题目前保留原书大写风格，若后续需要更统一的目录视觉，可再做标题格式化。

### 当日补充：浏览器插件常驻与网页自动翻译

- 用户需求：
  - 浏览器插件刷新网页后不要消失，要保持常驻
  - 支持网页自动翻译开关
  - 支持记录“当前网站不自动翻译”，下次访问时保持生效

#### 主要改动

- 调整扩展默认设置：
  - [`chrome-extension/lib/analyzer.js`](./chrome-extension/lib/analyzer.js)
  - 新增：
    - `autoTranslateOnLoad`
    - `disabledAutoTranslateHosts`
- 改造插件主逻辑：
  - [`chrome-extension/content-script.js`](./chrome-extension/content-script.js)
  - 主要内容：
    - 页面加载后默认注入常驻圆球
    - 刷新后自动恢复常驻入口
    - 设置页新增“自动翻译网页”开关
    - 设置页新增“当前网站自动翻译 / 不翻译”开关
    - 域名级持久化记录不自动翻译站点
    - 自动翻译仅在：
      - 开启自动翻译
      - 开启页内直译
      - 当前站点未被禁用
      的情况下执行
    - 面板 `×` 不再让插件完全消失，而是收起成常驻圆球，避免误判为插件失效

#### 验证结果

- 扩展脚本语法检查通过：
  - `node --check chrome-extension/content-script.js`
  - `node --check chrome-extension/background.js`
- 本地构建通过：
  - `npm run build`
- `npm run lint` 失败，但失败项均为项目内既有 React/TypeScript 旧问题，未发现本次浏览器插件改动新增报错

#### 经验总结

- 对浏览器插件而言，“关闭”如果会把入口一起隐藏，用户通常会理解成“扩展掉了”；常驻产品更适合采用“收起但保留入口”的交互。
- “自动翻译”应拆成两层控制：
  - 全局是否自动翻译
  - 当前站点是否排除
  这样既能保留默认自动化，也能避免在少数站点反复打扰用户。

### 当日补充：拼写游戏错词重拼逻辑修复

- 用户反馈：拼写游戏里单词即使出现过错误，只要当场改对就直接过题，没有被重新排到后面复拼。
- 修复文件：
  - [`src/pages/SpellingGame.tsx`](./src/pages/SpellingGame.tsx)
- 调整内容：
  - 只要某个单词本轮出现过拼错，或用户点过“正确拼写”，该词在当前题目纠正成功后会自动追加到队列末尾
  - 只有后续某一次“从头到尾无错误拼对”，才计入真正通过
  - 出现过错误的这次纠正，不再计入 `correctCount`，也不推进该词的复习进度
  - 正确提示文案改为明确提示“已加入后续重新拼写”
- 经验：
  - 拼写训练里“当前纠正成功”和“今日复习通过”是两件事，状态机必须拆开处理
  - 如果只改提示文案、不改队列流转，用户会立刻感知规则和结果不一致

### 当日补充：词汇口径拆分为“不会 / 今日待学 / 已掌握”

- 用户明确要求：
  - `new` 不等于“今日待学习”
  - 一个词只有完整走完艾宾浩斯周期，才算“已掌握”
  - 收藏词只要还没掌握，也属于“不会”
  - 若涉及 Supabase 结构改动，需要先沟通

#### 本次处理结论

- 本轮 **未修改 Supabase 表结构**
- 仅基于现有字段完成语义拆分：
  - `mastery_level < 5`：不会 / 未掌握
  - `mastery_level >= 5`：已掌握
  - `今日待学`：当日应学习的词，单独统计，不再等同于 `new`

#### 主要改动

- 词库筛选口径调整：
  - [`src/hooks/useVocabulary.ts`](./src/hooks/useVocabulary.ts)
  - `new` 过滤改为所有 `mastery_level < 5` 的词
  - `due` 过滤改为所有未掌握且 `next_review_at` 到期或为空的词
- 词库页文案调整：
  - [`src/lib/i18n.ts`](./src/lib/i18n.ts)
  - `生词 / New / 新出単語` 改为 `不会 / Unmastered / 未習得`
- 艾宾浩斯页统计调整：
  - [`src/pages/EbbinghausPage.tsx`](./src/pages/EbbinghausPage.tsx)
  - 顶部卡片改为：
    - 不会
    - 今日待学
    - 已掌握
  - 下方仍保留“新学 / 到期复习”的任务拆分
- 全局卡片学习改为读取今日应学词：
  - [`src/pages/FlashcardPage.tsx`](./src/pages/FlashcardPage.tsx)
  - 全局模式不再拉旧 `new`，改为拉 `due`
- 移除手动“标记已掌握”的捷径：
  - [`src/pages/VocabPage.tsx`](./src/pages/VocabPage.tsx)
  - [`src/pages/FlashcardPage.tsx`](./src/pages/FlashcardPage.tsx)
  - [`src/pages/LightningGame.tsx`](./src/pages/LightningGame.tsx)
  - [`src/pages/ListenIdentifyGame.tsx`](./src/pages/ListenIdentifyGame.tsx)
  - 现在只能通过完整复习流程进入 `mastery_level >= 5`

#### 经验总结

- “不会”是能力状态，“今日待学”是任务状态，这两个概念必须分层，否则统计、筛选、学习入口都会互相污染。
- 只要允许手动跳到 `mastered`，艾宾浩斯进度就会失真，后续所有复习预测都会被污染。
- 在不改数据库结构的前提下，当前用 `mastery_level` 和 `next_review_at` 已经足够表达这三个核心概念。

### 当日补充：桌面端截图翻译与 DMG 更新

- 用户需求：
  - 桌面端新增截图翻译
  - 交付新的 DMG 安装包
  - 交互参考系统级选区截图 + OCR + AI 翻译

#### 主要改动

- Electron 主进程新增截图 IPC：
  - [`electron/main.cjs`](./electron/main.cjs)
  - 通过 macOS `screencapture -i -x` 调起系统选区截图
  - 截图结果转为 `data:image/png;base64,...` 返回渲染层
  - 对取消截图和系统屏幕录制权限错误做了单独处理
- 预加载桥接新增桌面 API：
  - [`electron/preload.cjs`](./electron/preload.cjs)
  - 暴露：
    - `electronShell.platform`
    - `electronShell.captureScreenshot()`
- 新增桌面端截图翻译组件：
  - [`src/components/translate/DesktopScreenshotTranslator.tsx`](./src/components/translate/DesktopScreenshotTranslator.tsx)
  - 功能包括：
    - 选择截图区域
    - OCR 进度展示
    - 提取文字
    - AI 翻译
    - 译文覆盖预览
    - OCR 文本复制
    - 译文复制
    - 一键将 OCR 文本回填到普通文本翻译框
- 翻译页接入桌面端截图翻译：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 桌面端打开翻译页即可直接使用截图翻译，无需新增独立入口
- 新增桌面端全局类型声明：
  - [`src/types/electron.d.ts`](./src/types/electron.d.ts)

#### 验证结果

- Electron 脚本语法检查通过：
  - `node --check electron/main.cjs`
  - `node --check electron/preload.cjs`
- 主项目构建通过：
  - `npm run build`
- 桌面端打包通过：
  - `npm run desktop:build`
- 新 DMG 产物：
  - `release/Linswift-0.0.0-arm64.dmg`

#### 经验总结

- 截图选区如果自己实现一套桌面遮罩，开发和兼容成本都偏高；在 macOS 桌面端直接复用系统 `screencapture` 更稳。
- 截图翻译这类功能必须把“截图成功”“OCR 识别中”“AI 翻译中”拆成多段状态，否则用户会误判成卡死。
- OCR 和 PDF 扫描件本质是同一条能力链，复用已有 OCR 布局和覆盖渲染模型，能明显减少新功能成本。

### 当日补充：修复“今日待学有数，但打开学习为空”

- 用户反馈：
  - 艾宾浩斯看板显示“今日待学”有词
  - 打开卡片学习页却提示“今天没有待学习词汇了”

#### 原因定位

- 看板页统计口径是“今天整天的任务”：
  - 新词：`review_count = 0`
  - 复习：`next_review_at < 明天 00:00`
- 学习页此前读取的是 `due`：
  - `next_review_at <= 当前时间`
- 结果就是：
  - 今天稍后才到时间的复习词，会出现在看板里
  - 但此刻进入学习页时拿不到
  - 用户感知为“数字和实际学习列表不一致”

#### 主要改动

- 新增词库筛选口径：
  - [`src/hooks/useVocabulary.ts`](./src/hooks/useVocabulary.ts)
  - 新增 `today` filter，语义为“今天整天应学习的任务”
- 调整卡片学习页读取逻辑：
  - [`src/pages/FlashcardPage.tsx`](./src/pages/FlashcardPage.tsx)
  - 全局词库模式从 `fetchVocabulary('due')` 改为 `fetchVocabulary('today')`
  - 补充学习页加载态，避免查询过程中先闪“没有待学习词汇”

#### 验证结果

- 本地构建通过：
  - `npm run build`

#### 经验总结

- “到期”至少有两种口径：
  - 即时到期：`<= now`
  - 今日任务：`< tomorrow start`
- 看板、任务入口、学习页如果不统一口径，就一定会出现“上面有数、点进去为空”的产品错误。

### 当日补充：目录滚动、书籍封面、桌面侧栏与 Anne 新书入库

- 用户需求：
  - 修复章节目录抽屉无法滚动
  - 修复加入书架后的经典书封面缺失
  - 桌面端左侧固定栏新增设置入口，并在下方展示账户信息
  - 新增 `Anne_of_Green_Gables.txt`，清洗后上架
  - 在图书目录下补一份书籍处理与清洗说明

#### 主要改动

- 修复章节目录/书签/阅读设置抽屉滚动：
  - [`src/components/books/ClassicChapterReader.tsx`](./src/components/books/ClassicChapterReader.tsx)
  - 右侧抽屉容器改为 `flex` + `min-h-0` + `overflow-y-auto`
  - 章节目录现在可以在移动端和窄窗口里正常纵向滚动
- 修复“全部书籍”对公共经典书仍显示 emoji 兜底的问题：
  - [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)
  - 对带 `shared_book_slug` 的用户书架项，统一复用 `ClassicBookCover`
  - 最近阅读卡片也同步复用经典书封面
- 桌面端左侧固定栏补充设置与账户区：
  - [`src/components/reactbits/CardNav.tsx`](./src/components/reactbits/CardNav.tsx)
  - 固定栏底部新增“设置”入口
  - 在设置入口下方显示头像、昵称、账号邮箱
  - 账户卡点击后进入资料编辑页
- 新增 Anne 经典书配置：
  - [`src/data/classicBooks.ts`](./src/data/classicBooks.ts)
  - [`scripts/prepare-classic-books.mjs`](./scripts/prepare-classic-books.mjs)
  - 新增 `anne-of-green-gables` 元数据、封面主题与预处理规则
  - 章节格式使用 `chapter-inline`
- 更新经典书资源说明：
  - [`public/classic-books/README.md`](./public/classic-books/README.md)
  - [`public/classic-books/书籍处理说明.md`](./public/classic-books/书籍处理说明.md)
  - README 更新为 11 本
  - 新增中文说明文档，明确上架、清洗、切章和验证流程

#### 验证结果

- 经典书预处理通过：
  - `npm run prepare:classic-books`
  - 成功生成 11 本书的 processed JSON
- 主项目构建通过：
  - `npm run build`
- Anne 生成结果确认：
  - `public/classic-books/processed/anne-of-green-gables.json`
  - 共 39 章

#### 经验总结

- 公共经典书一旦进入用户书架，展示层不能退回通用 emoji 封面，否则“已下载”和“未下载”会像两套产品。
- 右侧抽屉类 UI 如果内部列表要滚动，父容器必须给出明确高度约束；只有 `overflow-y-auto` 不够。
- 新增一本经典书时，正文起点匹配比章节解析器更关键，先保证从正文而不是目录页开始，后面的切章才稳定。

### 当日补充：修复学习页图书馆漏改与封面比例失衡

- 用户反馈：
  - 学习页“图书馆”区域仍然显示旧 emoji 卡片
  - 书架“全部书籍”区域在桌面端书少时会把封面拉得过大
  - 不同经典书封面的主图形比例不一致，`Anne of Green Gables` 明显偏小

#### 主要改动

- 学习页图书馆统一复用经典书封面组件：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 对 `shared_book_slug` 书籍改用 `ClassicBookCover`
  - 点击经典书直接进入章节阅读页，不再错误跳到 `reading-prep`
- 阅读准备页统一共享经典书封面：
  - [`src/pages/ReadingPrepPage.tsx`](./src/pages/ReadingPrepPage.tsx)
  - 避免出现“书架有封面，点开准备页又变回 emoji”的断层
- 调整书架“全部书籍”桌面栅格：
  - [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)
  - 改为更密的响应式列数，并限制单卡最大宽度
  - 解决书少时封面被拉得过大
- 调整经典封面图形缩放：
  - [`src/components/books/ClassicBookCover.tsx`](./src/components/books/ClassicBookCover.tsx)
  - 新增按图案类型的缩放与位移修正
  - 把 `leaf` 图案放大并居中，同时收敛部分过大的图形

#### 验证结果

- 本地构建通过：
  - `npm run build`

#### 经验总结

- 只要同一本共享经典书在不同页面走了不同渲染路径，视觉风格一定会碎裂。
- 桌面大屏下，图书栅格如果不限制单卡最大宽度，书少时会天然放大设计缺陷。
- AI 封面里“图案大小一致”不是 SVG 统一 viewBox 就能解决，还需要按图形类型做视觉校准。

### 当日补充：学习设置页并入主题设置入口

- 用户反馈：
  - 桌面端“学习设置”工作台里还缺少主题设置
  - 主题相关能力不应该只留在个人页或独立页面里

#### 主要改动

- 在学习设置页新增主题设置卡：
  - [`src/pages/LearningSettingsPage.tsx`](./src/pages/LearningSettingsPage.tsx)
  - 直接接入 `useThemeSettings`
  - 支持在学习设置页内快速切换：
    - 浅色
    - 深色
    - 跟随系统
- 保留完整主题设置页入口：
  - 在主题卡内新增“打开完整主题设置”按钮
  - 继续跳转到 `/theme-settings` 做字体、语言、主题色等细调
- 桌面端与移动端同步补齐：
  - 桌面端出现在右侧工作台列
  - 移动端出现在复习制与功能开关之间

#### 验证结果

- 本地构建通过：
  - `npm run build`
- 生产环境已更新：
  - [https://linswift-app.vercel.app](https://linswift-app.vercel.app)

#### 经验总结

- “学习设置”和“主题设置”对用户来说都属于同一个控制台语义，桌面工作台里拆太散会增加寻找成本。
- 这类聚合页适合放“高频直接改”的主题项，而把低频细调留给完整设置页。

### 当日补充：词库“已会”图标恢复可点击反馈

- 用户反馈：
  - 词汇列表中的“已会/已掌握”图标点不动

#### 原因定位

- 此前为了避免用户手动跳过完整艾宾浩斯周期，词库里的 `BadgeCheck` 从按钮改成了纯展示 `span`
- 结果是状态语义保住了，但交互被完全拿掉，用户会误以为按钮坏了

#### 主要改动

- 恢复“已会”图标可点击：
  - [`src/pages/VocabPage.tsx`](./src/pages/VocabPage.tsx)
  - 桌面端和移动端列表都改回按钮
- 点击后不再直接把单词改成已掌握：
  - 仍然遵守“必须走完整个艾宾浩斯周期才能掌握”的规则
  - 改为弹出状态说明，展示：
    - 当前是否已掌握
    - 当前阶段
    - 累计复习次数
    - 下次复习时间
    - 为什么不能手动跳过
- 说明文案接入本地复习制：
  - 读取当前 7 天/15 天周期，保证提示和实际规则一致

#### 验证结果

- 本地构建通过：
  - `npm run build`
- 生产环境已更新：
  - [https://linswift-app.vercel.app](https://linswift-app.vercel.app)

#### 经验总结

- 纯展示图标和可点击图标在视觉上差异太小，去掉交互后必须同步改视觉，否则用户只会觉得“坏了”。
- 当产品规则不允许直接执行某个动作时，最稳妥的替代不是静默禁用，而是给出可点击的解释性反馈。

### 2026-03-21：语法模块三阶段重构

- 用户目标：
  - 不再停留在“语法树 + 模板课页 + 纯选择题”
  - 把语法学习路径、知识内容和练习方法一起升级
  - 可以利用现有长难句模块形成回流
  - 允许进入 Supabase 结构升级

#### 阶段一：前端内容与课页重构

- 新增统一语法蓝图数据层：
  - [`src/data/grammarCatalog.ts`](./src/data/grammarCatalog.ts)
  - 为 24 个语法节点补齐：
    - 课程摘要
    - 3 段学习单元（规则骨架 / 场景辨析 / 易错修正）
    - 高频例句
    - 混合练习
    - 输出工作坊
    - 相关长难句映射
- 重写语法课页：
  - [`src/pages/GrammarLessonPage.tsx`](./src/pages/GrammarLessonPage.tsx)
  - 从“知识点 + 选择题”改成：
    - 路径式单元说明
    - 对比与易错展示
    - 多题型练习：
      - 单选
      - 填空
      - 改错
      - 输出改写
    - 外部资源区
    - 长难句联动区
- 重写语法树页：
  - [`src/pages/GrammarTreePage.tsx`](./src/pages/GrammarTreePage.tsx)
  - 不再写死“3 个知识点 / 3 道测试题”
  - 改为显示：
    - 节点层级
    - 节点簇
    - 核心题数量
    - 输出练习数量
    - 掌握度
    - 待复习数
    - 顶部弱项概览

#### 阶段二：Supabase 结构升级

- 新增语法 V2 migration：
  - [`supabase-migration-grammar-v2.sql`](./supabase-migration-grammar-v2.sql)
- 新增数据表：
  - `grammar_units`
  - `grammar_examples`
  - `grammar_exercises`
  - `grammar_attempts`
  - `grammar_review_queue`
  - `grammar_error_stats`
- 新增错因统计函数：
  - `increment_grammar_error_stat(...)`
- 新增最小可用 seed：
  - [`supabase-seed-grammar-v2.sql`](./supabase-seed-grammar-v2.sql)
  - 作用：
    - 先把新结构跑起来
    - 即使 Supabase 还没导入本地完整蓝图，也不会是空表

#### 阶段三：自适应复习与长难句回流

- 新增语法学习引擎：
  - [`src/lib/grammar.ts`](./src/lib/grammar.ts)
  - 支持：
    - 练习评估
    - 错因标签统计
    - 节点掌握度
    - 到点复习队列
    - 本地持久化
    - 尝试同步到 Supabase（若新表已存在）
- 新增语法学习 hook：
  - [`src/hooks/useGrammarLearning.ts`](./src/hooks/useGrammarLearning.ts)
- 长难句阅读页新增回链语法课：
  - [`src/pages/LongSentenceReadingPage.tsx`](./src/pages/LongSentenceReadingPage.tsx)
  - 支持从语法课跳到指定长难句，也支持从长难句跳回相关语法点
- AI 拆句页新增语法课建议：
  - [`src/pages/LongSentenceAnalyzePage.tsx`](./src/pages/LongSentenceAnalyzePage.tsx)
  - 根据句法提示词和 grammar points 推荐对应语法课

#### 验证结果

- 本地构建通过：
  - `npm run build`
- 当前前端策略：
  - 优先使用本地 richer grammar catalog
  - 若 Supabase 新表尚未迁移，仍可正常运行
  - 若 Supabase 已迁移，则会开始同步 grammar attempts / error stats

#### 经验总结

- 语法模块真正的升级不在于多塞几篇资源，而在于把“规则、辨析、改错、输出、复习、阅读联动”放进同一条数据链路。
- 页面里的临时模板一旦多起来，后续几乎无法做自适应学习，所以必须先抽出统一 grammar blueprint 层。
- Supabase 结构升级不能卡住前端改造，最稳的做法是：
  - 前端先支持 richer local model
  - 云端再逐步接管持久化与统计

### 当日补充：阅读器悬浮词卡拆分“我会了 / 我不会”

- 用户反馈：
  - 章节阅读器里的悬浮单词卡只有“我会了”
  - 实际点击后却会把词加入陌生词库，语义完全反了
  - 用户需要明确的双选项：
    - 我会了
    - 我不会

#### 主要改动

- 修正经典书章节阅读器悬浮词卡：
  - [`src/components/books/ClassicChapterReader.tsx`](./src/components/books/ClassicChapterReader.tsx)
  - 点击高亮词时不再直接调用 `addWord`
  - 底部按钮改成双按钮：
    - `我会了`
      - 只标记当前阅读会话中该词已掌握
      - 不再加入个人陌生词库
    - `我不会`
      - 明确调用 `addWord`
      - 将该词加入个人词库做后续学习
- 同步修正旧阅读页单词弹窗：
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - 保持和经典书章节阅读器一致，避免两个阅读入口行为不同

#### 验证结果

- 本地构建通过：
  - `npm run build`

#### 经验总结

- 词卡按钮的文案和实际写库动作必须一一对应，否则用户会直接失去对词汇系统的信任。
- “会了/不会”这种二元判断必须在用户确认后再落库，不能在打开弹窗时就偷偷执行自动收录。

### 当日补充：重定义“自动收录词汇”开关，并发布生产

- 用户反馈：
  - 既然词卡里已经有明确的 `我会了 / 我不会`
  - 底部的“自动收录词汇”开关就不能再继续表达“点词就自动入库”
  - 这个开关需要重定义成不和词卡冲突的新语义

#### 主要改动

- 将阅读器中的 `autoCollect` 重新定义为：
  - `识词后自动加入词库`
  - 含义：
    - 开启：AI 识别出的重点词会整批自动加入个人词库
    - 关闭：只高亮和展示释义，由用户逐个点 `我不会` 再加入
- 同步改动经典书章节阅读器：
  - [`src/components/books/ClassicChapterReader.tsx`](./src/components/books/ClassicChapterReader.tsx)
  - 识词完成后：
    - 若开关开启，调用 `addWords` 批量加入词库
    - 状态文案改成“已识别 X 个重点词，并自动加入词库”
    - 若关闭，则提示“等待你逐个判断”
  - 设置面板中的文案也同步改成新语义，并补了一句说明
- 同步改动旧阅读页：
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - 保持与经典书阅读器一致，避免不同阅读入口逻辑分裂

#### 验证结果

- 本地构建通过：
  - `npm run build`
- 生产环境已更新：
  - [https://linswift-app.vercel.app](https://linswift-app.vercel.app)
  - 本次生产实例：
    - 已迁移到 `icecream` 名下 Vercel 项目，旧的 `wsammes-projects` 实例地址不再使用

#### 经验总结

- 一旦产品同时存在“显式人工判断”和“自动批量收录”，两者的职责必须彻底分开。
- 最稳的拆法是：
  - 单词级动作交给词卡按钮
  - 批量级动作交给自动化开关

### 当日补充：接入 DeepL 服务端代理，翻译页支持 DeepL / AI / 混合模式

- 用户目标：
  - 不再只依赖单个 AI prompt 同时完成“翻译 + 生词提取”
  - 评估并接入 DeepL 作为更稳定的主翻译引擎
  - 保留 AI 在陌生词分析上的优势
  - 在前端提供明确的模式切换：
    - DeepL
    - AI
    - 混合

#### 主要改动

- 新增 DeepL 服务端代理：
  - [`api/deepl/translate.js`](./api/deepl/translate.js)
  - 通过 Vercel Function 调用 DeepL `v2/translate`
  - 认证改为服务端 `DEEPL_API_KEY`
  - 前端不直接暴露 DeepL 密钥
  - 同时兼容 `file://` 桌面端与跨域调用所需的 CORS 头
- 新增翻译编排层：
  - [`src/services/translation.ts`](./src/services/translation.ts)
  - 统一实现三种模式：
    - `hybrid`
      - DeepL 负责主译文
      - AI 负责陌生词识别
    - `deepl`
      - 只使用 DeepL，强调稳定可预期
    - `ai`
      - 只使用 Moonshot AI，保留更灵活的表达
  - 当 DeepL 不可用时自动回退到 AI，并在结果中附带提示说明
  - 对桌面端和 `localhost` 做了代理基址兜底，避免本地调试时 `/api` 404
- 翻译页 UI 升级：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 新增三段式模式切换卡片
  - 将模式持久化到 `localStorage`
  - 翻译结果卡新增来源说明：
    - 主翻译来源
    - 生词分析来源
  - DeepL 回退、AI 提词失败等情况会以提示文案展示，而不是静默吞掉
- 环境变量说明补充：
  - [`.env.example`](./.env.example)
  - 新增 `DEEPL_API_KEY`
  - 新增可选 `DEEPL_API_BASE`
  - 新增可选 `VITE_DEEPL_PROXY_BASE_URL`

#### 验证计划

- 计划执行：
  - `npm run build`
  - 本地模拟调用 `api/deepl/translate.js`

#### 经验总结

- “稳定翻译”和“学习分析”最好分层：
  - DeepL 更适合给主译文
  - AI 更适合做生词与语境解释
- 把多个职责塞进一个大 prompt，短期省事，长期最容易在 JSON、术语、稳定性上一起失控。

### 当日补充：浏览器插件同步支持 DeepL / AI / 混合模式

- 用户目标：
  - 既然主站翻译页已经支持 `DeepL / AI / 混合`，网页插件的设置也要同步
  - 插件内的网页 / 字幕翻译不能继续停留在旧的单一路径
  - popup 与网页内悬浮面板的设置语义要保持一致

#### 主要改动

- 扩展默认设置新增翻译模式：
  - [`chrome-extension/lib/analyzer.js`](./chrome-extension/lib/analyzer.js)
  - 新增 `translationMode: 'hybrid'`
- 扩展翻译库升级：
  - [`chrome-extension/lib/ai.js`](./chrome-extension/lib/ai.js)
  - 新增 DeepL 批量翻译调用，默认走线上 `https://www.linswift.com/api/deepl/translate`
  - `translateBatchLines` 现在支持：
    - `hybrid`
    - `deepl`
    - `ai`
  - DeepL 不可用时会自动回退到 AI / 公共翻译，并返回提示说明
- 背景页转发设置：
  - [`chrome-extension/background.js`](./chrome-extension/background.js)
  - `panel-translate-lines` 现在会带上扩展设置中的 `translationMode`
- 网页内悬浮面板设置页新增引擎切换：
  - [`chrome-extension/content-script.js`](./chrome-extension/content-script.js)
  - 在“语言与识别”区域新增网页 / 字幕翻译引擎下拉框
  - 支持 `混合模式 / DeepL / AI`
  - 切换后 YouTube 字幕整段翻译缓存会重置并按新模式重新拉取
  - 状态文案与 YouTube 卡片会显示当前模式与实际翻译来源
- 插件 popup 同步新增设置：
  - [`chrome-extension/popup.html`](./chrome-extension/popup.html)
  - [`chrome-extension/popup.js`](./chrome-extension/popup.js)
  - popup 里也可以直接保存当前翻译引擎，避免和网页面板配置分裂
- DeepL 服务端代理补充批量模式：
  - [`api/deepl/translate.js`](./api/deepl/translate.js)
  - 现在同时兼容：
    - 单条 `text`
    - 批量 `texts`
  - 浏览器插件字幕翻译可以批量调用，不需要逐行请求

#### 验证结果

- 已执行：
  - `node --check chrome-extension/content-script.js`
  - `node --check chrome-extension/background.js`
  - `node --check chrome-extension/popup.js`
  - `node --check chrome-extension/lib/ai.js`
  - `node --check api/deepl/translate.js`
  - `npm run build`

#### 经验总结

- 插件的“翻译设置”不能只改 UI，不改链路；否则用户能选模式，但实际仍走旧翻译器。
- 字幕翻译这种批量场景要优先支持服务端批量代理，否则逐条调用 DeepL 会在时延和成本上都很差。

### 当日补充：网页端学习设置页删减无效区块并重排布局

- 用户目标：
  - 学习设置页信息过载
  - 有些区域只是说明或重复摘要，并不是真正的配置项
  - 需要删掉没有实际配置意义的部分，再把真正可用的设置集中起来

#### 主要改动

- 重构学习设置页：
  - [`src/pages/LearningSettingsPage.tsx`](./src/pages/LearningSettingsPage.tsx)
  - 将“每日目标 + 复习周期”合并为同一张 `学习计划` 卡
  - 保留 `学习模式` 为独立卡片
  - 将 `发音设置 / 自动播放单词 / 显示例句 / 复习提醒` 合并到 `朗读与阅读`
  - 精简 `主题设置`，只保留常用模式切换和进入完整主题设置的入口
- 删除无实际配置价值的内容：
  - 桌面端左侧大段说明文案改成真正的“同步状态”
  - 删除重复性的 `Current Plan` 展示风格，改成紧凑的当前生效摘要
  - 删除学习设置页里的 `学习统计` 跳转项，避免把导航入口伪装成设置项
- 布局优化：
  - 桌面端改为两列主内容，左侧放计划与模式，右侧放阅读开关与主题
  - 移动端与桌面端保持同样的分组逻辑，减少认知切换

#### 验证计划

- 计划执行：
  - `npm run build`

#### 经验总结

- 设置页最容易失控的不是“功能不够”，而是把说明、摘要、导航、配置混在一起。
- 真正的优化通常先做减法：先删掉假设置，再讨论视觉排布。

### 当日补充：官网上架浏览器插件安装包并新增配置教程页

- 用户目标：
  - 将网页插件安装包直接放到官网下载
  - 官网提供一页完整的安装与配置教程
  - 首页需要有明显入口，而不是只靠用户自己找扩展目录

#### 主要改动

- 新增浏览器插件教程页：
  - [`src/pages/BrowserExtensionPage.tsx`](./src/pages/BrowserExtensionPage.tsx)
  - 内容包含：
    - zip 安装包下载入口
    - Chrome / Edge 手动安装步骤
    - 翻译模式说明（混合 / DeepL / AI）
    - 常见问题和使用建议
- 官网路由接入：
  - [`src/App.tsx`](./src/App.tsx)
  - 新增公开路由 `/browser-extension`
- 首页增加插件入口：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
  - 顶部导航加入 `插件教程`
  - Hero 区新增浏览器插件按钮与下载提示卡
  - 功能网格增加 `浏览器插件` 卡片
  - 页脚资源区新增：
    - 插件安装教程
    - 浏览器插件下载
- 插件安装包上架到官网静态资源：
  - [`public/downloads/linswift-browser-extension.zip`](./public/downloads/linswift-browser-extension.zip)
  - 供官网直接下载

#### 验证计划

- 计划执行：
  - `npm run build`
  - 生产部署到 Vercel

#### 经验总结

- “官网已支持插件”不能只停留在口头说明，必须同时具备下载资产、清晰入口和安装文档，用户才能真正完成安装。
- 插件这类离线安装包适合放到 `public/downloads`，这样官网、教程页和后续更新都能复用同一稳定路径。

### 当日补充：引入 GSAP 学习流程动画，强调 AI 辅助学习闭环

- 用户目标：
  - 在官网和学习首页增加更有“学习过程感”的动画
  - 不做噱头式飞入特效，而是展示 AI 如何参与识别、讲解、练习和复习闭环
  - 动画要兼顾性能和可访问性，避免滚动卡顿或强迫用户观看

#### 主要改动

- 官网新增 AI 学习流程动画区：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
  - 在 Hero 与功能区之间插入 `AIWorkflowSection`
  - 使用 `GSAP + ScrollTrigger` 做滚动触发的分步 reveal
  - 以 4 个阶段展示：
    - AI 识别输入内容
    - 拆词与语境讲解
    - 生成个性化练习
    - 回流到复习闭环
  - 增加流动轨迹与状态 chip，强化“学习管线”而不是单点工具的感知
- 学习首页加入节奏化入场动画：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - Banner、分组标题、任务卡、书卡、右侧信息卡分层进入
  - 桌面端给 AI 引言卡增加轻微浮动，提升首页活力但不影响信息阅读
- 采用 GSAP 最小必要组合：
  - 全局已安装 skills：
    - `gsap-core`
    - `gsap-timeline`
    - `gsap-react`
    - `gsap-scrolltrigger`
    - `gsap-performance`

#### 动画约束

- 优先使用 `transform` / `opacity`，避免布局抖动
- 对 `prefers-reduced-motion` 做显式降级
- ScrollTrigger 只在关键区块使用一次性 reveal，不做满页滥用
- React 中所有 GSAP 作用域都限制在组件根节点内，确保 cleanup 明确

#### 验证计划

- 计划执行：
  - `npm run build`
  - 页面滚动检查官网流程区是否自然进入
  - 检查学习首页在桌面和移动端是否无闪烁、无卡顿

#### 经验总结

- 学习产品里的动画价值，不在“炫”，而在于把抽象的 AI 能力转换成用户能感知的连续步骤。
- 真正有用的动画应该服务叙事和层级，而不是和内容抢注意力。

### 当日补充：官网加入 Stripe 接入前的免费 / 付费分层计划

- 用户目标：
  - 在正式接入 Stripe 前，先想清楚如何区分免费用户和付费用户
  - 这套方案不是只写在内部文档里，而是要透明放到官网，避免用户误以为已经支持自动订阅

#### 主要改动

- 重写官网定价区：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
  - 将原本偏“已接入订阅”的 `Pro 月付 / 7 天试用` 话术改成更符合当前状态的：
    - 免费版
    - Pro 内测版
    - 团队试点
- 新增 `Pre-Stripe Plan` 说明区：
  - 详细说明 Stripe 上线前的账号分层逻辑
  - 包括：
    - 所有用户默认进入免费版
    - Pro 用户通过邮件申请 + 人工审核开通
    - 权益按账号身份记录，多端共享
    - Stripe 正式接入后的迁移承诺
- 增加“层级 / 适合人群 / 开通方式 / 当前计费方式 / 权限边界”的公开表格
- 保留明确的 `mailto:aw@linswift.com` 入口，方便申请 Pro 内测和团队试点

#### 经验总结

- 在支付系统未接入前，最容易损伤信任的不是“暂时不能自动付费”，而是官网文案假装已经支持完整订阅。
- 先把用户层级、功能边界和迁移承诺透明写出来，比先摆一个漂亮但不真实的价格卡更重要。

### 当日补充：开始开发听力板块的内置静态内容与播放器能力

- 用户目标：
  - 听力板块不能继续只依赖 Supabase 里的少量种子数据
  - 即使数据库为空，也要内置可直接使用的静态听力内容
  - 播放器要真正可用，至少保证播放、暂停、切换、倍速这些核心能力稳定

#### 主要改动

- 新增内置听力内容数据源：
  - [`src/data/listeningContent.ts`](./src/data/listeningContent.ts)
  - 按 `study / news / ted / course / music` 分类提供首批内置素材
  - 提供 `mergeListeningContent()`，统一合并云端内容与本地静态内容
- 升级听力播放器 Hook：
  - [`src/hooks/useAudioPlayer.ts`](./src/hooks/useAudioPlayer.ts)
  - 补齐：
    - `playbackRate`
    - `setPlaybackRate`
    - 更稳定的暂停 / 恢复
    - 倍速下的时间进度更新
- 听力首页改成“内置 + 云端”混合数据：
  - [`src/pages/ListeningHubPage.tsx`](./src/pages/ListeningHubPage.tsx)
  - 不再提示“必须先往 listening_content 导数据”才能使用
- `随行听` 页面开始可直接使用内置内容，并加入倍速切换：
  - [`src/pages/ListenGoPage.tsx`](./src/pages/ListenGoPage.tsx)
- `听·图书馆` 页面同样切到混合内容源，并加入倍速控制：
  - [`src/pages/ListenLibPage.tsx`](./src/pages/ListenLibPage.tsx)
- `听歌填字` 页面补充了内置 music 内容来源和速度切换：
  - [`src/pages/ListenFillPage.tsx`](./src/pages/ListenFillPage.tsx)

#### 当前结论

- 这轮先把“能用起来”排在第一位：
  - 内容不再空白
  - 播放器不再只是一个会念句子的壳
- 仍然使用浏览器 TTS 做首版音频承载，后续如果要更自然的真人音频资源，再补独立音频文件和真实 waveform 播放链路

#### 验证计划

- 计划执行：
  - `npm run build`
  - 重点检查三类页面是否都能在无 Supabase 内容的情况下显示内置素材
  - 检查播放 / 暂停 / 上一句 / 下一句 / 倍速切换是否可用

#### 经验总结

- 对内容型模块来说，“先把数据结构做对”比先做更多页面皮肤更重要。
- 对播放器来说，最常见的问题不是“功能不够多”，而是状态机不稳；所以优先保证播放、暂停、恢复和倍速切换的连贯性。

### 2026-03-22：修复 PDF OCR 翻译覆盖层排列不整齐

- 用户问题：
  - PDF OCR 翻译后的覆盖文本仍然排列不整齐
  - 体感上像是回填文本“漂移”或“自己重新换行”，而不是简单的识别错误

#### 主要改动

- 调整 OCR 翻译区域选择策略：
  - [`src/pages/PDFReaderPage.tsx`](./src/pages/PDFReaderPage.tsx)
  - 由原先优先使用 `translationRegions` 的段落合并框，改为优先使用 `lines` 推导出的逐行区域
  - 为逐行区域增加“同列下一行”约束，限制当前行的可扩展高度，避免翻译块向下吞并下一行空间
- 移除渲染阶段的跨块字号平滑：
  - 之前会把相邻翻译块的字号强行拉近，导致独立行框之间互相干扰
  - 现在保留各自拟合结果，不再做全局抹平
- 禁止浏览器对已计算好的翻译行再次自动换行：
  - OCR 覆盖层的 `.ocr-overlay-line` 和 `.ocr-overlay-line-text` 统一改为 `white-space: nowrap`
  - 去掉 `word-break / overflow-wrap / text-wrap: pretty`
  - 这样翻译文本只按 `fitTranslatedRegionText()` 预计算的断行结果展示，不会出现浏览器二次回排

#### 当前结论

- 这次修的是“翻译覆盖层回填策略”，不是重新训练 OCR
- 问题主因更像是：
  - 段落合并框过大
  - 手工算好行后又被 CSS 再次换行
  - 相邻块字号被错误平滑
- 修复后，扫描版 PDF 的 OCR 翻译应该会明显更贴近原始行位置信息

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过，未新增 TypeScript / Vite 构建错误

#### 经验总结

- OCR 排版问题经常不在“识别错了”，而在“识别结果被后处理破坏了”。
- 对扫描版 PDF 来说，优先保住逐行锚点，比追求段落级自动重排更稳定。

### 2026-03-22：翻译页补齐词典型结果展示

- 用户目标：
  - 翻译功能不能只停留在“整句翻译 + 陌生词列表”
  - 单词查询时至少要像词典一样展示：
    - 联想候选
    - 音标 / 发音
    - 词性义项
    - 例句
    - 近义词 / 相关词

#### 主要改动

- 扩展单词详情结构：
  - [`src/services/gemini.ts`](./src/services/gemini.ts)
  - `WordDetail` 新增：
    - `phoneticBr`
    - `phoneticAm`
    - `partOfSpeechBlocks`
    - `relatedWords`
  - AI 提示词也改成返回更接近词典的数据结构，而不是只有一行 meaning
- 翻译页新增词典结果面板：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 对“短英文查询 / 单词型查询”自动触发 `getWordDetail()`
  - 在整段翻译结果之外，补了一块独立词典卡片
  - 包含：
    - 顶部 query 结果标题
    - 候选词 / 相关词快速跳转
    - 英 / 美音标和发音按钮
    - `简明 / 例句 / 相关词` 三个标签页
    - 按词性分组的义项展示
    - 记忆提示

#### 当前结论

- 现在翻译页开始分成两条路径：
  - 句子 / 段落：继续走翻译卡
  - 单词 / 短词组：额外补出词典视图
- 这比原来“任何输入都只回一段翻译”更接近真实英语学习场景

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 翻译页和词典页如果完全分离，用户会频繁在“查义 / 看例句 / 收藏”之间跳转，体验会断裂。
- 更合理的方式是：翻译页保留整句能力，同时在“单词型输入”时自动升级为轻词典视图。

### 2026-03-22 词典联想列表收紧 + 新标签结构

#### 主要改动

- 优化顶部候选词展示：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 将原本较松散的联想卡片改成更接近词典搜索建议的紧凑列表
  - 自动补入 AI 返回的 `relatedWords`，并与历史/词库联想去重
- 细化词典标签页：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 标签从 `简明 / 例句 / 相关词` 调整为：
    - `简明`
    - `词组`
    - `例句`
    - `百科`
  - `词组` 展示高频搭配和短语
  - `百科` 整合近义词、百科说明、联想词，避免“相关词”标签过于单薄
- 扩展单词详情字段：
  - [`src/services/gemini.ts`](./src/services/gemini.ts)
  - AI 返回结构新增：
    - `phrasePatterns`
    - `encyclopedia`
  - 同时增强回退数据，保证 AI 异常时页面不空

#### 当前结论

- 现在翻译页顶部联想词更像传统词典的搜索建议，而不是一组松散卡片。
- 单词查询页也从“查义 + 例句”扩展成了更完整的轻词典结构，已经开始具备“继续深挖”的能力。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 词典类 UI 的联想区越紧凑越好，用户要的是快速扫一眼并切词，不是阅读一组视觉卡片。
- “词组 / 例句 / 百科”这种标签组织更接近真实查词路径，也更利于后续继续补牛津式说明、词源或搭配频率。

### 2026-03-22 翻译页升级为完整词典布局

#### 主要改动

- 将词典结果从单卡片升级为更接近有道 / 欧路的查词页结构：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 上方保留检索结果头部、发音与操作区
  - 中部改成 `标签主内容 + 右侧信息栏` 的双栏布局
- 补充词典级交互：
  - 词条支持直接 `加入词库`
  - 支持 `复制单词`
  - 右侧新增：
    - 词条信息
    - 继续查词
    - 学习建议
- 强化标签区的词典感：
  - `简明` 改成按词性编号展示
  - `词组` 改成固定搭配列表
  - `例句` 改成编号例句区
  - `百科` 改成近义词、百科说明、联想词三段式

#### 当前结论

- 现在翻译页已经不只是“翻译结果里嵌一个词典卡片”，而是开始像一个真正的轻量词典页。
- 词典、发音、收藏、继续查词、学习动作已经放进同一套流里，查词路径更顺。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 真正接近词典产品，不是堆更多内容，而是把信息分层：头部识别、主释义、扩展信息、继续探索。
- 右侧信息栏虽然简单，但对桌面端很关键，它能把“查词”和“学习动作”从正文里剥离出来。

### 2026-03-23 生产环境路由空白修复

#### 主要改动

- 修复网页端点击跳转后偶发白屏、必须手动刷新才能恢复的问题：
  - [`src/main.tsx`](./src/main.tsx)
  - 启动时检查遗留的 `Service Worker / CacheStorage`
  - 如果命中旧缓存控制器，自动执行一次安全重载，不再要求用户手动刷新
- 减少同站整页跳转：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
  - [`src/pages/BrowserExtensionPage.tsx`](./src/pages/BrowserExtensionPage.tsx)
  - 将官网内的“插件教程”入口改回 React Router 站内导航，减少再次走旧缓存 HTML 的机会

#### 当前结论

- 这次修复不只是“关掉 PWA”，而是补了旧缓存迁移期的兜底逻辑。
- 对已经访问过旧站版本的用户，首次进入新版本时会自动替他们完成那次必要刷新。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 只关闭新版本 PWA 并不能立刻解决线上用户手里的旧 Service Worker；迁移阶段必须有一次性的清理和重载策略。
- 同站教程页、详情页这类内容页，能走 SPA 导航就不要再用整页跳转，否则最容易放大缓存不一致问题。

### 2026-03-23 移动端学习页图书馆卡片缩小

#### 主要改动

- 调整学习页移动端图书馆卡片尺寸：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 卡片宽度由 `148px` 收窄到 `124px`
  - 封面比例改得更紧凑，减少视觉占比
  - 卡片内边距、圆角、封面圆角、emoji 封面字号同步收敛
  - 标题字号与行高压缩，避免封面和标题一起显得过大

#### 当前结论

- 现在移动端学习页里的“图书馆”更接近推荐位而不是 mini 书架，首屏信息密度更合理。
- 这次只动学习页移动端，不影响桌面端和书架页的封面展示。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 学习首页的图书推荐位应该服务于“快速进入阅读”，而不是复刻完整书架卡片。
- 在移动端，封面面积一旦过大，任务卡和图书馆之间的视觉层级会被打乱。

### 2026-03-23 移动端底部功能栏改为悬浮固定

#### 主要改动

- 调整移动端主应用底部导航为悬浮固定层：
  - [`src/components/layout/AppShell.tsx`](./src/components/layout/AppShell.tsx)
  - 底栏改为 `fixed` 浮层，并为主内容区补充底部安全间距
- 优化底部导航视觉：
  - [`src/components/layout/BottomNav.tsx`](./src/components/layout/BottomNav.tsx)
  - 从贴边条形导航改为圆角浮层导航
  - 激活态增加更明显的胶囊高亮
- 修正翻译页在移动端的高度策略：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 移除页面自身额外撑满 `100dvh` 的做法，避免和外层滚动容器叠加

#### 当前结论

- 现在手机端底部功能栏会一直悬浮可见，不需要因为“最近翻译”很多而一路滑到底才能切页面。
- 这次修复是全局性的，不只改善翻译页，学习、词库、个人中心都会受益。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 移动端底部主导航应当属于“系统层”，不应随页面内容一起滚走。
- 页面内部如果再自己声明一层 `100dvh`，很容易和壳层滚动机制冲突，尤其在带底栏的布局里。

### 2026-03-23 学习页移动端图书馆二次缩小

#### 主要改动

- 继续压缩学习页移动端图书馆推荐位：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 卡片宽度进一步缩到 `104px`
  - 横向间距、封面区域、标题字号继续收紧
- 调整经典书封面的紧凑态：
  - [`src/components/books/ClassicBookCover.tsx`](./src/components/books/ClassicBookCover.tsx)
  - `compact` 状态下的：
    - 外层圆角
    - 顶部 `AI Cover` 标记
    - 底部作者区
    - 标题字号
  - 全部同步缩小，避免外框缩了但封面内部信息仍显拥挤

#### 当前结论

- 现在移动端学习页里的图书推荐更像“快速阅读入口”，不会再呈现迷你书架的大面积压迫感。
- 这次只影响学习页推荐位和紧凑态封面，不影响桌面端正常书封展示。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 缩小移动端书卡不能只改外层宽度，否则封面内部标题区和标记区会继续显大。
- 对推荐位卡片，内部信息必须同步进入 `mini` 视觉规格，整体才会真正变轻。

### 2026-03-23 词库与图书馆安全跳转兜底

#### 主要改动

- 新增安全导航工具：
  - [`src/lib/navigation.ts`](./src/lib/navigation.ts)
  - 对网页端的 `/app/vocab` 与 `/bookshelf` 做路由白名单
  - 命中白名单时改走完整文档导航，避免再次出现 SPA 切换后白屏、用户必须手动刷新的问题
- 接入统一回退逻辑：
  - [`src/hooks/useLogicalBack.ts`](./src/hooks/useLogicalBack.ts)
  - 返回链路如果落到词库或书架，也自动使用安全跳转
- 替换所有核心入口：
  - [`src/components/layout/BottomNav.tsx`](./src/components/layout/BottomNav.tsx)
  - [`src/components/reactbits/CardNav.tsx`](./src/components/reactbits/CardNav.tsx)
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - [`src/pages/ListenLibPage.tsx`](./src/pages/ListenLibPage.tsx)
  - [`src/pages/ReadingPrepPage.tsx`](./src/pages/ReadingPrepPage.tsx)
  - [`src/pages/ReadingPage.tsx`](./src/pages/ReadingPage.tsx)
  - [`src/pages/PDFReaderPage.tsx`](./src/pages/PDFReaderPage.tsx)
  - [`src/pages/VocabTestPage.tsx`](./src/pages/VocabTestPage.tsx)
  - [`src/pages/SpellingGame.tsx`](./src/pages/SpellingGame.tsx)
  - [`src/pages/ListenIdentifyGame.tsx`](./src/pages/ListenIdentifyGame.tsx)
  - [`src/pages/WordMatchGame.tsx`](./src/pages/WordMatchGame.tsx)
  - [`src/pages/LightningGame.tsx`](./src/pages/LightningGame.tsx)

#### 当前结论

- 这次先把“词库”和“图书馆”两条最常用、最容易出现空白的路径从 SPA 跳转改为网页端完整文档导航。
- 用户点击这两个入口时，即使仍存在某个尚未完全定位的前端运行时差异，也不会再停留在空白页等待手动刷新。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 对已经确认在生产环境会偶发白屏、且刷新后一定恢复的少数路由，优先用“安全跳转兜底”比继续让用户手动刷新更值。
- 这类问题在根因未完全定位前，不应再只靠缓存清理兜底，而要直接修入口层体验。

### 2026-03-23 学习页移动端任务区与图书馆间距修复

#### 主要改动

- 调整学习页移动端版式节奏：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - `今日任务` 区块底部外边距从通用值改为移动端更大的 `mb-7`
  - `图书馆` 标题区在移动端增加轻微顶部留白
  - 横向书卡滚动区补 `pt-1` 与更宽松的 `pb-3`

#### 当前结论

- 移动端 `今日任务` 与 `图书馆` 不再紧贴，任务卡阴影和书卡区域不会再发生视觉重叠。
- 这次只修布局间距，不改任务卡内容和图书馆功能。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 移动端横向滚动列表前，如果上一组卡片本身带明显阴影，section 间距要比桌面端更保守。
- 只调标题 margin 往往不够，滚动容器本身也需要额外的上下缓冲。

### 2026-03-23 个人页学习时长改为词汇量等级记录

#### 主要改动

- 个人页统计卡片改版：
  - [`src/pages/ProfilePage.tsx`](./src/pages/ProfilePage.tsx)
  - 移除不稳定的 `学习时长` 展示
  - 第三张卡改为读取最近一次 `词汇量测试` 结果
  - 展示内容改为：
    - 当前等级 `Lv.x`
    - 最近一次词汇量数值
    - 最近测试日期
  - 如果还没做过测试，则显示“未测试 / 去测试”
  - 点击卡片可直接跳到词汇量测试页
- 补充国际化文案：
  - [`src/lib/i18n.ts`](./src/lib/i18n.ts)
  - 新增：
    - `profile_vocab_level`
    - `profile_vocab_level_empty`
    - `profile_vocab_level_cta`

#### 当前结论

- 个人页现在不再展示一个没有持续更新来源的学习时长字段。
- 改成词汇量等级记录后，统计卡片终于对应到了真实可验证的数据源。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 个人页统计区不应展示“理论上应该有、实际上没人维护”的指标。
- 如果一个统计字段没有稳定写入链路，优先换成已有真实数据，而不是继续保留装饰性数字。

### 2026-03-23 学习页移动端今日任务标题间距回调

#### 主要改动

- 微调学习页移动端 `今日任务` 标题与四宫格的距离：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 仅将移动端标题下边距从 `mb-3` 收回到 `mb-2`

#### 当前结论

- `今日任务` 标题不再显得和下方四宫格分离过远。
- 这次只回收标题下方空白，不影响前面已经修好的 `图书馆` 分区间距。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 移动端 section 节奏不能只靠一个通用间距值；标题和卡片之间、卡片组和下一 section 之间，往往需要分开调。

### 2026-03-23 主工作区安全跳转范围扩大

#### 主要改动

- 扩大网页端安全跳转白名单：
  - [`src/lib/navigation.ts`](./src/lib/navigation.ts)
  - 从原先只覆盖：
    - `/app/vocab`
    - `/bookshelf`
  - 扩大为覆盖整个主工作区：
    - `/app/translate`
    - `/app/learn`
    - `/app/vocab`
    - `/app/profile`
    - `/bookshelf`

#### 当前结论

- 现在从其他页面点击 `翻译` 也会走完整文档导航，不再复用那条偶发白屏的 SPA 路由切换。
- 这是一层体验兜底，优先保证线上“点得开”，后续再继续追根因。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 当白屏问题已经从个别页面扩散到主工作区标签时，继续一个个补名单价值不高，应直接把整组主入口统一切到稳定路径。

### 2026-03-23 学习页标题与子卡片间距统一

#### 主要改动

- 统一移动端 `今日任务` 与 `图书馆` 标题到子卡片的垂直距离：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 将 `今日任务` 标题下边距调整为与 `图书馆` 相同的 `mb-3.5`

#### 当前结论

- 现在 `今日任务` 和 `图书馆` 这两个 section，标题到各自子卡片的距离是一致的。
- 统一以 `图书馆` 当前的节奏为准，不再出现一个贴得更近、一个更远的情况。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 同一页面里并列 section 的标题节奏最好统一，否则即使数值差异不大，移动端观感也会很明显。

### 2026-03-23 词库页背景改为纯白

#### 主要改动

- 移除词库页的玻璃底色与装饰背景：
  - [`src/pages/VocabPage.tsx`](./src/pages/VocabPage.tsx)
  - 桌面端不再使用 `glass-page` 与顶部橙色径向光晕
  - 移动端也去掉背景网格与渐变层
  - 页面底色统一改为纯白 `bg-white`

#### 当前结论

- 词库页现在和你要的方向一致，页面背景是纯白底，保留卡片层级但不再有整页玻璃背景氛围。

#### 验证结果

- 已执行：
  - `npm run build`
- 构建通过

#### 经验总结

- 页面整体背景和卡片质感要分开控制；如果用户要“纯白背景”，就不该继续保留整页渐变和装饰网格，即使卡片还保留玻璃风格。

### 2026-03-23 翻译页移动端语言选择器文字居中修复

#### 主要改动

- 修复翻译页移动端顶部语言选择器文字偏移：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)
  - 为源语言和目标语言 `select` 增加 `appearance-none`
  - 补充横向内边距 `px-3`
  - 使用 `[text-align-last:center]` 强制当前选中项文字居中显示

#### 当前结论

- 移动端翻译页顶部的双语言选择器现在会和容器居中对齐，不再出现文本偏左、视觉不平衡的问题。

#### 验证结果

- 待执行：
  - `npm run build`
  - 生产部署

#### 经验总结

- 原生 `select` 在移动端浏览器里经常带默认箭头和内边距，单纯 `text-center` 不够，通常还需要配合 `appearance-none` 与 `text-align-last` 才能真正把已选文本居中。

### 2026-03-23 网页端功能页安全跳转统一修复

#### 主要改动

- 扩大网页端安全导航覆盖范围：
  - [`src/lib/navigation.ts`](./src/lib/navigation.ts)
  - 现在所有受保护功能页路由都走文档级跳转，避免 SPA 切换命中旧资源后白屏
- 将各功能中心页的一级入口改为统一安全跳转：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - [`src/pages/EbbinghausPage.tsx`](./src/pages/EbbinghausPage.tsx)
  - [`src/pages/ListeningHubPage.tsx`](./src/pages/ListeningHubPage.tsx)
  - [`src/pages/SpeakingHubPage.tsx`](./src/pages/SpeakingHubPage.tsx)
  - [`src/pages/GrammarTreePage.tsx`](./src/pages/GrammarTreePage.tsx)
  - [`src/pages/VocabGamePage.tsx`](./src/pages/VocabGamePage.tsx)
  - [`src/pages/SceneSelectPage.tsx`](./src/pages/SceneSelectPage.tsx)
  - [`src/pages/LongSentenceHubPage.tsx`](./src/pages/LongSentenceHubPage.tsx)
  - [`src/pages/BookshelfPage.tsx`](./src/pages/BookshelfPage.tsx)
  - [`src/components/reactbits/CardNav.tsx`](./src/components/reactbits/CardNav.tsx)
- 修复移动端翻译页语言选择器文字未居中：
  - [`src/pages/TranslatePage.tsx`](./src/pages/TranslatePage.tsx)

#### 当前结论

- 现在从学习页今日任务、左侧导航、书架、听力/口语/语法/游戏中心继续进入子功能页时，不应再出现“先空白、手动刷新才加载”的问题。

#### 验证结果

- 已执行：
  - `npm run build`
  - 生产部署
- 待补充：
  - 线上点击回归验证

#### 经验总结

- 对当前这个项目而言，网页端受保护功能页在 PWA 旧缓存与大包切换的组合下，SPA 跳转并不稳定；比起零散补洞，更适合统一走文档级导航。

### 2026-03-23 学习页移动端横屏热度图居中修复

#### 主要改动

- 修复学习页移动端横屏下学习热度图未居中的问题：
  - [`src/pages/LearnPage.tsx`](./src/pages/LearnPage.tsx)
  - 将移动端热度图外层增加 `flex justify-center`

#### 当前结论

- 现在手机版本横屏时，学习热度图会在卡片区域内居中显示，不再贴左。

#### 验证结果

- 待执行：
  - `npm run build`
  - 生产部署

#### 经验总结

- 对固定尺寸的网格组件，移动端横屏下如果外层仍是普通块级流式布局，视觉上很容易偏左；这类模块更适合由父容器显式控制居中。
## 2026-03-23 插件划词与品牌 Logo 统一

- 将官网 `BrandLogo` 的展示源从旧 `brand-logo.png` 切换到新的 `brand-logo.svg`。
- 更新图标生成脚本，统一从 `src/assets/brand-logo.svg` 生成网页 favicon、PWA 图标、桌面端图标和浏览器扩展图标。
- 为 `chrome-extension/manifest.json` 补齐 `icons` 与 `action.default_icon`，让插件工具栏图标跟随新 Logo。
- 修复浏览器插件“鼠标划词”只支持单一 text node 的问题，改成基于选区矩形的覆盖高亮，兼容更多真实网页结构。
- 计划在构建后重新生成官网下载的扩展安装包，避免官网 ZIP 与源码逻辑脱节。

## 2026-03-23 桌面端 DMG 重打包

- 基于最新桌面资源重新执行 `npm run desktop:build`。
- 确认新的 DMG 已包含新 Logo、最新网页资源、OCR 资产和经典书处理产物。
- 生成本地安装包：`release/Linswift-0.0.0-arm64.dmg`。

## 2026-03-23 桌面截图翻译增强

### 开发目标

- 让桌面端截图翻译在截图区域旁边直接查看译文，而不是只看底部纯文本输出。
- 支持绑定全局截图快捷键。
- 支持配置 OCR 完成后是否自动把提取文本写入剪贴板。
- 重新打包本地 DMG，确保这些能力直接可安装使用。

### 主要改动

- Electron 主进程新增桌面截图设置持久化：
  - [`electron/main.cjs`](./electron/main.cjs)
  - 使用 `userData/desktop-screenshot-settings.json` 保存：
    - `shortcut`
    - `autoCopyText`
    - `previewMode`
- 新增桌面截图相关 IPC：
  - `desktop:get-screenshot-settings`
  - `desktop:update-screenshot-settings`
  - `desktop:write-clipboard-text`
- 新增全局快捷键注册与触发：
  - 默认快捷键：`CommandOrControl+Shift+2`
  - 触发后会唤起主窗口并直接开始系统截图流程
- 预加载桥接扩展：
  - [`electron/preload.cjs`](./electron/preload.cjs)
  - [`src/types/electron.d.ts`](./src/types/electron.d.ts)
- 桌面截图翻译组件重构：
  - [`src/components/translate/DesktopScreenshotTranslator.tsx`](./src/components/translate/DesktopScreenshotTranslator.tsx)
  - 新增“截图翻译设置”区块
  - 支持两种预览模式：
    - `桌面弹窗`
    - `覆盖译文`
  - 截图后在原图上用编号标注识别区域，并在应用内保留对应编号译文卡片作为预览
  - OCR 完成后可自动复制提取文本到剪贴板
  - 快捷键输入框支持直接按组合键录入，不必手动拼字符串
- 新增桌面翻译悬浮窗：
  - [`electron/overlay-preload.cjs`](./electron/overlay-preload.cjs)
  - [`electron/translation-overlay.html`](./electron/translation-overlay.html)
  - 截图翻译完成后会在系统桌面上弹出置顶小窗，定位在截图操作结束时的鼠标附近，直接显示区域编号与译文，脱离主 App 查看。
- 修复全局快捷键唤起目标：
  - 全局截图快捷键现在始终回到主窗口，不会误把悬浮译文窗当成主窗口。

### 验证结果

- 已执行：
  - `npm run build`
  - `npm run desktop:build`
- 构建结果：
  - 前端生产构建通过
  - 桌面 DMG 打包通过

### 产物记录

- 本地 DMG：
  - `release/Linswift-0.0.0-arm64.dmg`
- 生成时间：
  - `2026-03-23 20:34:49`
- SHA-256：
  - `32f52eda6b49ae18566ade6543dce237da248a7d193c50f672ebf1f91e0d5e7b`

### 遗留问题

- 当前截图快捷键仍依赖 Electron accelerator 字符串规则；如果用户输入非常规组合键，可能注册失败，需要继续补更强的输入校验和错误提示。
- 当前桌面弹窗会定位在截图结束时的鼠标附近，已经脱离主 App，但还没有精确贴合实际截图矩形边界；后续如果要更强贴边，需要补选区坐标获取链路。

### 经验总结

- 桌面截图类功能不要只停留在前端组件层，必须把“设置持久化、全局快捷键、剪贴板写入”一起下沉到 Electron 主进程，否则功能很容易只在当前会话看起来可用。
- 对 OCR 结果做“区域编号 + 桌面悬浮译文窗”比直接大面积覆盖原图更稳，也更适合扫描件和排版复杂的截图场景。

## 2026-03-23 官网公开页链接白屏修复

### 开发目标

- 修复官网公开页之间互相跳转时偶发白屏、必须手动刷新才能恢复的问题。
- 覆盖：
  - 浏览器插件教程页
  - 登录页
  - 注册页
  - 法律页
  - 官网首页各锚点与 footer/header 内部链接

### 主要改动

- 统一官网内部链接跳转逻辑：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
- 新增内部站点链接拦截函数：
  - 对 `/login`、`/register`、`/browser-extension`、`/legal/*`、`/#features` 这类站内地址统一转为 React Router 导航
  - 保留 `mailto:`、外部下载地址和外站链接为原生跳转
- `Navbar` 与 `Footer` 不再对官网内部链接直接使用整页加载：
  - Header 导航
  - 移动端菜单卡片
  - Footer 的产品/资源/法律链接
- 首页新增基于 `location.hash` 的滚动恢复：
  - 通过 `navigate('/#features')` 进入首页后，仍可自动滚动到对应区块

### 当前结论

- 官网公开页之间现在优先走站内路由，不再触发整页加载，因此应明显减少“点击后白屏，刷新才恢复”的问题。

### 验证结果

- 已执行：
  - `npm run build`
  - 生产部署

### 经验总结

- 对当前项目来说，官网公开页本身不应该复用“功能页安全整页跳转”策略。公开页如果继续混用原生 `<a href="/...">`，就会把已经修好的 SPA 入口再次拉回整页加载路径。

## 2026-03-24 插件悬浮位置刷新漂移修复

### 开发目标

- 修复浏览器插件常驻圆球 / 悬浮面板拖动后刷新网页回到默认角落的问题。

### 主要改动

- 在 [`chrome-extension/content-script.js`](./chrome-extension/content-script.js) 中新增悬浮位置持久化：
  - 拖拽结束后写入 `chrome.storage.local`
  - 刷新页面后自动恢复上次位置
  - 依据左右 / 上下锚点恢复，不再死记绝对坐标
- 在窗口尺寸变化、全屏切换、最小化 / 展开时，对悬浮位置重新做越界矫正，避免面板跑出可视区域。

### 验证结果

- 已执行：
  - `node --check chrome-extension/content-script.js`

### 经验总结

- 这种悬浮 UI 不能只改 `style.left/top`，否则一刷新就会丢失状态。持久化时直接保存“离哪条边更近”和边距，比死存像素坐标更稳。

## 2026-03-24 官网公开页初次进入白屏修复（二次收敛）

### 开发目标

- 修复 `browser-extension`、`login`、`register`、法律页等公开页面从站内点击进入时初次白屏、手动刷新后才恢复的问题。

### 主要改动

- 在 [`src/lib/navigation.ts`](./src/lib/navigation.ts) 中把公开站点入口也纳入“文档级跳转”范围：
  - `/`
  - `/login`
  - `/register`
  - `/browser-extension`
  - `/legal/*`
- 将官网和公开页之间的入口统一切换到 `navigateSafely(...)`：
  - [`src/pages/LandingPage.tsx`](./src/pages/LandingPage.tsx)
  - [`src/pages/BrowserExtensionPage.tsx`](./src/pages/BrowserExtensionPage.tsx)
  - [`src/pages/LoginPage.tsx`](./src/pages/LoginPage.tsx)
  - [`src/pages/RegisterPage.tsx`](./src/pages/RegisterPage.tsx)
  - [`src/pages/AboutPage.tsx`](./src/pages/AboutPage.tsx)

### 当前结论

- 对当前项目的官网公开页来说，最稳的策略不是继续赌 SPA 切换，而是统一回到整页进入。用户手动刷新能恢复，本质上已经说明目标页面的独立加载链路是正常的。

## 2026-03-24 桌面截图翻译浮层与 DMG 白屏修复

### 开发目标

- 优化桌面截图翻译浮层的视觉表达，让它更像系统级 OCR 识别结果卡片。
- 让弹窗位置从“贴在鼠标旁边”改成更接近截图选区下侧的展示方式。
- 修复 DMG 安装后打开桌面应用白屏的问题。

### 主要改动

- 在 [`index.html`](./index.html) 中恢复 Vite 正常入口：
  - 删除误写进源码的构建产物脚本与样式引用
  - 改回 `src/main.tsx` 作为入口
  - 将 `favicon` 与 `apple-touch-icon` 改为 `%BASE_URL%` 路径，确保桌面 `file://` 构建能产出相对资源
- 在 [`electron/main.cjs`](./electron/main.cjs) 中调整截图翻译浮层定位：
  - 横向不再固定贴右侧，而是更接近选区下方居中
  - 纵向优先显示在锚点下侧，空间不足时再回退到上侧
- 在 [`electron/translation-overlay.html`](./electron/translation-overlay.html) 中加强浮层设计：
  - 新增橙色顶部标记 `OCR 识别区域`
  - 保留深灰 OCR 原文区、译文区、词汇学习区与底部状态控件
  - 继续保留“点按复制”逻辑，不引入独立复制按钮

### 验证结果

- 已执行：
  - `npm run build:desktop`
  - `npm run desktop:build`
- 已确认打包进 `app.asar` 的 `dist/index.html` 资源引用为相对路径：
  - `./favicon.png`
  - `./assets/...`

### 经验总结

- Electron 白屏最常见的根因不是主进程本身，而是前端入口文件被错误写成了线上构建产物。`file://` 模式下只要残留 `/assets/...` 绝对路径，打包后几乎必白。
- 如果希望“截图框停留在原位并持续显示 OCR 状态”，就不能继续依赖系统 `screencapture -i` 的黑盒交互。必须自己接管选区框选，拿到精确矩形后，再用 `screencapture -R` 和独立指示层来完成截图与加载反馈。

## 2026-03-24 桌面端设置页设计稿收紧

### 开发目标

- 继续在 [`pencil-new.pen`](./pencil-new.pen) 现有稿上迭代桌面端设置页，不重建布局。
- 统一学习设置、主题设置、发音设置三块的尺寸、对齐、文案和 glass design 语言。
- 让设置页更像一套紧凑的桌面控制面板，而不是松散的信息拼贴。

### 主要改动

- 重建了设置页第二个顶层画板的内部结构，保留原有暖灰底、橙色高亮、轻玻璃面板风格。
- 学习设置区：
  - 收紧了顶部摘要卡与学习计划卡的高度、间距和按钮尺寸
  - 将“同步状态 / 当前生效 / 朗读与阅读”统一为更紧凑的摘要卡
  - 将每日目标、复习周期改成更小巧的控制按钮
- 主题设置区：
  - 补全并细化了主题摘要、当前状态、外观模式、界面语言、字体大小、主题色
  - 统一改为中文标题
  - 确保 `日本语` 完整显示
  - 将三种外观模式收紧为更精致的三按钮结构
- 发音设置区：
  - 新增整块发音设置主面板
  - 增加试听按钮、发音类型、朗读速度、音量调节、播放偏好
  - 将底部播放偏好整理成两行横向排列

### 验证结果

- 已确认 [`pencil-new.pen`](./pencil-new.pen) JSON 结构合法，可正常解析。
- 已做文件级检查：
  - 关键中文文案已存在
  - `PERSONAL SETTINGS` 等旧英文标题已移除
  - `日本语` 已保留
- Pencil MCP 截图与布局检查仍然不可用，原因是本机 Pencil app 未连上 MCP WebSocket。

### 经验总结

- `.pen` 设计稿在 MCP 不可用时，仍可通过 JSON 节点级重构完成大部分布局优化，但像素级截图验收仍然依赖设计工具连通。
- 对这种带大量卡片和控件的桌面面板，最稳的策略是先统一外层版心和卡片高度，再收缩子控件，而不是先微调字体与颜色。

## 2026-03-24 截图翻译可视化实验室

### 开发目标

- 为桌面截图翻译建立无需反复打 DMG 的本地可视化迭代入口。
- 先在网页端调选区框、加载态和浮层结构，再进入 Electron 校验系统层定位。

### 主要改动

- 新增 [`src/pages/DesktopScreenshotLabPage.tsx`](./src/pages/DesktopScreenshotLabPage.tsx)，模拟网页背景、OCR 选区框、状态条和翻译浮层。
- 在 [`src/App.tsx`](./src/App.tsx) 中增加公开调试路由 `/dev/screenshot-lab`。
- 实验室支持切换：
  - OCR 中 / 翻译中 / 完成
  - 弹窗在选区下方 / 上方
  - 智慧识词开关

### 当前结论

- 现阶段最适合的可视化开发路径不是继续打 DMG，而是：
  1. `npm run dev`
  2. 打开 `/dev/screenshot-lab` 调结构与层级
  3. 视觉和交互稳定后，再用 `npm run desktop:start:dev` 看 Electron 实际定位
  4. 最后才打 DMG

## 2026-03-24 设置页同步到网页 App 与 DMG

### 开发目标

- 将 `pencil-new.pen` 中收紧后的桌面设置页样式真正同步进网页 App。
- 让桌面版 DMG 直接复用同一套前端结构，而不是停留在设计稿阶段。

### 主要改动

- 重构 [`src/pages/LearningSettingsPage.tsx`](./src/pages/LearningSettingsPage.tsx) 的桌面分支布局：
  - 顶部统一为 `个人设置 / 学习设置` 标题区
  - 上半区收成 `学习设置 + 主题设置` 双栏主面板
  - 下半区补成完整 `发音设置` 主面板
- 学习设置中加入：
  - `同步状态`
  - `当前生效`
  - `学习计划`
- 主题设置中加入：
  - `主题摘要`
  - `当前状态`
  - `外观模式`
  - `界面语言`
  - `字体大小`
  - `主题色`
- 发音设置中加入：
  - `试听说明`
  - `当前状态`
  - `发音类型`
  - `朗读速度`
  - `音量调节`
  - `播放偏好`
- 补齐 `useTTSSettings()` 的桌面端调用方法，接入：
  - `setAccent`
  - `setRate`
  - `setVolume`
  - `previewVoice`

### 验证结果

- `npx tsc -b` 通过。
- `npx vite build` 通过。
- `npm run desktop:build` 通过，桌面 DMG 已重新生成。

### 经验总结

- 设计稿同步到真实产品时，先保证桌面分支功能方法完整接通，再收视觉差异，效率明显更高。
- 网页端和 Electron 共用同一套 React 页面时，优先在网页里把结构跑通，再打 DMG，可以大幅减少来回安装验证成本。

## 2026-03-24 截图翻译浮层位置与交互修复

### 开发目标

- 修正桌面截图翻译浮层与 OCR 选区重叠的问题。
- 让浮层底部语言条支持真正的交互切换，而不是静态文案。
- 统一实验室页与 Electron 实际浮层的视觉和交互行为。

### 主要改动

- 调整 [`electron/main.cjs`](./electron/main.cjs)：
  - 浮层改为不可拖动，避免位置漂移。
  - 选区弹窗优先锚定在选区左侧并严格放到选区下边线以下。
  - 下方空间不足时才回退到上方。
- 更新 [`electron/translation-overlay.html`](./electron/translation-overlay.html)：
  - 删除浮层左上角重复的 `OCR 识别区域` 标记。
  - 给右上关闭按钮留出独立顶部安全区，避免与内容重叠。
  - 底部语言 pill 改成可点击切换。
  - 底部 pill 文本字号提升到和译文更接近的级别。
- 扩展 Electron 通信：
  - [`electron/preload.cjs`](./electron/preload.cjs)
  - [`electron/overlay-preload.cjs`](./electron/overlay-preload.cjs)
  - [`src/types/electron.d.ts`](./src/types/electron.d.ts)
  - 新增浮层请求切换目标语言的 IPC 链路。
- 更新 [`src/components/translate/DesktopScreenshotTranslator.tsx`](./src/components/translate/DesktopScreenshotTranslator.tsx)：
  - 增加截图翻译的本地目标语言状态。
  - 响应浮层语言切换事件后重新翻译 OCR 结果。
  - 将语言切换同步回翻译页主状态。
- 同步更新 [`src/pages/DesktopScreenshotLabPage.tsx`](./src/pages/DesktopScreenshotLabPage.tsx)，使网页实验室页与实际桌面浮层结构一致。

### 验证结果

- `npx tsc -b` 通过。
- `npx vite build` 通过。
- `npm run desktop:build` 通过，DMG 已重新生成。

### 经验总结

- 截图翻译这类系统浮层如果既能拖动又依赖鼠标锚点，用户感知上会非常“飘”；优先锚定到选区几何位置更稳定。
- 语言切换这类交互不应只改浮层文案，必须打通到主渲染进程重新翻译，否则体验会像假按钮。

## 2026-03-24 学习设置页字号与深色主题修复

### 开发目标

- 修复桌面学习设置页字体过大的问题，提升信息密度和可读性。
- 修复深色模式下学习设置仍保持浅色背景的问题，统一跟随主题变量。

### 主要改动

- 调整 [`src/pages/LearningSettingsPage.tsx`](./src/pages/LearningSettingsPage.tsx) 桌面分支字号：
  - 页面主标题、模块标题、说明文、按钮字号整体下调一档
  - 学习计划、主题设置、发音设置三块统一压缩高度与文字层级
- 将桌面分支中写死的浅色背景替换为主题变量：
  - 主容器、主面板、子卡片改为 `var(--color-background) / var(--color-card) / var(--color-background-secondary)` 体系
  - 保留橙色作为状态强调色
- 同步优化底部桌面通用卡片组件：
  - `DesktopInfoCard`
  - `DesktopPlainCard`
  - `DesktopToggleTile`

### 验证结果

- `npx tsc -b` 通过。
- `npx vite build` 通过。
- 生产部署成功并别名到 `https://www.linswift.com`。

### 经验总结

- 桌面页如果直接使用固定浅色透明白，会破坏主题系统；应优先走主题变量，避免深色模式“局部失效”。
- 大号标题在信息密集设置页会造成视觉噪音，控制层级比放大字号更重要。
